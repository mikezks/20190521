import { FlightBooking.Selectors } from './flight-booking.selectors';

describe('FlightBooking.Selectors', () => {
  it('should create an instance', () => {
    expect(new FlightBooking.Selectors()).toBeTruthy();
  });
});
